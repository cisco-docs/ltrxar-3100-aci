# Overwrite default values here - this file is included in all NAC runs

defaults:
  apic:
    auto_generate_access_leaf_switch_interface_profiles: true
    tenants:
      bridge_domains:
        unicast_routing: true
